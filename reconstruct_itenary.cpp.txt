class Solution {
public:
    unordered_map<string, multiset<string>> adj;
    vector<string> result;

    void dfs(string src) {
        while (!adj[src].empty()) {
            string next = *adj[src].begin();
            adj[src].erase(adj[src].begin());
            dfs(next);
        }
        result.push_back(src); // postorder
    }

    vector<string> findItinerary(vector<vector<string>>& tickets) {
        for (auto &t : tickets) {
            adj[t[0]].insert(t[1]);
        }
        dfs("JFK");
        reverse(result.begin(), result.end());
        return result;
    }
};
we do not shrink a interval while doing stuff we just include intervals while working thats how we work 
result.back()[1]=max or something like this 
sort(intervals.begin(),intervals.end())
result.back()[1] this firstly takes the first thingy of the thingy and then accesses the second thingy of the backmost one 
