class Solution {
public:
    int majorityElement(vector<int>& nums) {
        int candidate=0,count=0;
        for(int x:nums){
            if(count==0)candidate=x;
            (candidate==x?count+=1:count+=-1);

        }return candidate;
    }
};